import { DatetimelineDirective } from './datetimeline.directive';

describe('DatetimelineDirective', () => {
  it('should create an instance', () => {
    const directive = new DatetimelineDirective();
    expect(directive).toBeTruthy();
  });
});
